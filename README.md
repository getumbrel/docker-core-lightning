# Core Lightning for Umbrel

A temporary Docker image built specifically for the Core Lightning app on
Umbrel, using Core Lightning's official signed release binaries for the current
security update.

The app will return to the official upstream image in a future update.
